{
  "name": "containers",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node main.js",
    "build:host": "rm -rf ../bin/* && pkg . -t host --output ../bin/container-api",
    "build:all": "rm -rf ../bin/* && pkg --out-path ../bin/ ."
  },
  "bin": "main.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "install": "^0.13.0",
    "morgan": "^1.10.0",
    "pkg": "^4.4.8"
  }
}
